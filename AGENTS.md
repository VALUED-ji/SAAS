# 装修管家 SaaS 开发与数据安全规范

本文件用于约束后续所有代码助手和开发人员在本项目中的开发行为。项目是装修公司业务管理 SaaS，涉及客户、工地、报价合同、定额模板、材料订单、组织权限、财务收款等敏感业务数据。任何改动都必须优先保证数据安全、权限正确、业务连续和可回滚。

最后更新：2026-07-18

## 1. 项目定位

- 本系统是装修公司内部业务管理系统，不是营销网站。
- 核心用户包括老板、门店负责人、设计师、项目经理、财务、材料/订单人员、系统管理员。
- 核心数据包括客户资料、手机号、房屋地址、服务团队、报价、合同、收款、退款、变更单、工地资料、材料库存、供应商、组织权限。
- 所有开发必须默认这些数据具有业务敏感性，不能随意展示、导出、删除、覆盖或记录到日志。

## 2. 最高优先级红线

以下行为默认禁止，除非用户明确要求并已经完成备份和风险确认：

- 不允许删除数据库、客户数据、合同数据、收款数据、工地资料、上传附件。
- 不允许执行 `git reset --hard`、批量删除、覆盖数据库、清空表等不可逆操作。
- 不允许把 `.env`、密钥、Token、数据库连接串、短信/地图/支付密钥写入前端代码或提交到仓库。
- 不允许为了修 UI 改动业务数据结构、接口返回含义或权限逻辑。
- 不允许绕过权限校验，只在前端隐藏按钮不能算安全。
- 不允许真实财务记录硬删除；错误收款、退款、冲销必须保留可追溯流水。
- 不允许在控制台、接口响应、报错信息中泄露手机号、地址、Token、数据库路径、SQL 明细、服务器路径。
- 不允许直接把电脑端复杂大表硬塞进移动端或小程序端，必须重新按移动场景设计。

## 3. 每次开发前必须确认

开始修改前先判断：

- 这次需求是 UI 调整、文案调整、业务逻辑、权限逻辑、数据结构，还是接口性能优化。
- 是否涉及客户、财务、合同、权限、删除、导入、导出、批量操作。
- 是否需要备份当前文件或数据库。
- 是否会影响已有页面、已有数据、已有用户操作流程。
- 是否只是用户要求的范围，不能擅自增加新功能、新字段、新入口。

如果需求表达模糊，但改错会影响数据或权限，必须先和用户确认。

## 4. 数据安全规范

### 4.1 敏感数据

以下数据视为敏感数据：

- 客户姓名、手机号、微信号、身份证、房屋地址、门牌号。
- 合同金额、报价金额、定金、工程款、退款、收款流水。
- 工地照片、付款凭证、合同附件、设计图纸。
- 员工账号、角色、组织归属、权限范围。
- 系统 Token、密钥、数据库文件、第三方接口配置。

处理规则：

- 非必要不展示完整敏感信息。
- 手机号必须按权限决定是否脱敏。
- 财务金额必须保持精度，不得用展示格式反向参与计算。
- 导出功能必须做权限校验和操作记录。
- 上传文件必须限制类型、大小和访问权限。

### 4.2 权限与数据范围

所有接口必须服务端校验权限，前端限制只能作为体验优化。

必须校验：

- 用户是否登录。
- 用户是否拥有当前功能权限。
- 用户是否能访问当前客户/工地/合同/收款数据。
- 门店、组织、负责人、服务团队、其他人员等数据范围。
- 管理权限和服务权限不能混淆。

典型规则：

- “我服务的”应按客户服务团队判断，包括其他人员。
- “我管理的”应按当前用户负责的门店/组织及下级组织判断。
- 不是服务团队成员时，手机号等敏感信息应脱敏。
- 即使前端没有按钮，接口也必须拒绝无权限操作。

### 4.3 财务数据

财务相关改动必须保守处理。

- 收款、退款、冲销、抵扣都必须保留流水。
- 收款金额允许大于当前期剩余应收时，必须按明确规则自动流转到后续款项。
- 冲销不是删除原记录，而是新增反向记录并标记关联关系。
- 退款金额、历史退款金额、定金抵扣、变更单退款抵扣、实际应收、已收金额、剩余应收的计算要清晰可追溯。
- 金额展示不得随意四舍五入到整数，财务中心默认保留完整两位小数。
- 财务中心金额展示按当前系统规范：不显示 `¥`，不使用千分位逗号。
- 时间统一按北京时间展示和录入。

## 5. 数据库与接口规范

### 5.1 接口输入

所有新增/修改接口必须：

- 校验登录态。
- 校验权限和数据范围。
- 使用结构化校验，优先使用 `zod` 或已有校验模式。
- 校验必填字段、金额、日期、枚举、ID 是否有效。
- 不信任前端传入的用户身份、组织身份、金额计算结果。

### 5.2 数据写入

涉及多表写入、库存扣减、收款流转、审批状态变更时：

- 必须使用事务或等价的原子性处理。
- 必须避免部分成功导致数据不一致。
- 必须处理重复提交、重复审批、重复收款。
- 必须返回清晰错误信息，不能暴露内部堆栈和 SQL。

### 5.3 删除策略

业务数据默认使用停用、作废、软删除、冲销、归档，不优先硬删除。

硬删除前必须确认：

- 是否有客户、合同、报价、订单、财务流水、附件依赖。
- 是否会破坏历史统计。
- 是否已有备份。
- 是否用户明确要求永久删除。

## 6. 前端开发规范

### 6.1 UI 一致性

本系统当前视觉方向：

- 成熟 SaaS 后台，不做营销页风格。
- 简洁、清晰、耐看、业务密度适中。
- 后台产品主色统一使用 `#407AFF`，只用于主按钮、激活态、焦点态和关键信息，不做大面积蓝色铺底；登录页等独立品牌页面按其页面规范执行。
- 页面底色使用 `#F6F7F9`，主面板使用白色，次级区域使用 `#FAFAF9`，边框统一使用 `#E7E9EE` 附近的低对比中性灰。
- 财务、列表、表格页面应清晰、克制，不使用花哨渐变和夸张阴影。
- 主内容面板默认 `16px` 圆角，弹窗默认 `18px` 圆角，输入框、下拉、普通按钮默认 `10px` 圆角，状态标签保留胶囊形；同类组件必须统一。
- 新页面优先复用 `src/components/ui/system-ui.tsx`，旧页面逐步由 `src/app/system-ui.css` 的系统作用域收敛，禁止再增加互相覆盖的全局补丁。

开发规则：

- 不擅自增加用户没有要求的新功能或装饰模块。
- 不擅自删除字段，除非用户明确要求。
- 文案必须使用系统已有统一叫法。
- 列表和详情页字段命名要统一。
- 按钮大小、圆角、阴影、下拉选择器应和系统现有规范一致。
- 用户明确说“只改颜色/文案/位置”时，不改布局和业务逻辑。

#### 6.1.1 UI 统一不是“看起来差不多”

后续所有 UI 规范化必须按同类组件的实际标准执行，不能只统一颜色或大概风格。

- 改动前必须先确认当前系统里最接近的参照页面或组件。例如客户管理列表、客户详情、工地详情、财务中心列表等。
- 同一类组件跨模块必须统一到具体尺寸：高度、`min-height`、内边距、圆角、边框颜色、背景色、字体大小、字体粗细、图标尺寸、激活态、悬停态。
- 如果客户管理和工地管理使用同一类 Tab 栏，不能一个按钮高、一个按钮矮；必须逐项核对外层容器、内层按钮、图标、文字、padding 和圆角。
- 如果截图反馈“高度不一致”“圆角不一致”“不对齐”，不能凭肉眼说已经一致；应优先检查代码中的实际 class 和 CSS 覆盖，必要时用截图像素或浏览器测量确认。
- 不要只通过全局 CSS 强行覆盖来假装统一。能复用同一组件或同一组 class 时，优先复用；如果使用作用域 CSS，必须确认不会被 Tailwind class、后续选择器或页面缩放再次覆盖。
- 同一模块里不要混用多套圆角体系。例如外层卡片、Tab 外壳、信息块、图标块、按钮应使用同一套明确层级，而不是 `rounded-lg`、`rounded-xl`、`rounded-[16px]` 随意混用。
- 普通内容卡片默认不加夸张阴影；只有弹窗、浮层、下拉面板这类需要层级感的组件才允许使用轻量阴影。

#### 6.1.2 当前高频组件标准

以下组件已经多次出现一致性问题，后续改动必须特别核对：

- 顶部状态筛选/分段按钮：外层、内层按钮、数字角标必须和当前客户管理列表保持一致，不允许同类按钮高度不同。
- 详情页 Tab 栏：客户详情和工地详情必须保持同一高度、同一圆角、同一图标尺寸、同一激活态蓝色按钮。
- 详情信息块：字段信息块外层圆角、边框、浅底色、左侧图标块尺寸和圆角必须统一。
- 服务团队模块：客户详情和工地详情必须使用同一类结构和视觉语言；不能一个页面是统一人员卡片，另一个页面又做成“主责大卡 + 彩色角色卡”。
- 列表白卡片：客户管理、工地管理、报价合同、财务中心、定额管理、订单管理等列表容器的白底、边框、圆角、底部分页区域必须保持一致。
- 弹窗和下拉选择器：全系统圆角、高度、文字垂直居中、浮层层级必须一致，不能出现系统原生风格和自定义风格混杂。

#### 6.1.3 UI 文案必须让业务用户看懂

后台 UI 文案必须面向门店、财务、设计师、项目经理等真实用户，不能暴露代码或内部状态感。

- 不使用用户看不懂的内部词，如“已配置”“系统默认”“来自工地”这类没有上下文的提示。
- 同样含义要改成业务可理解文案，例如“已配置”可写为“已加入团队”，“系统默认”可写为“默认顾问”，“来自工地”可写为“工地负责人”。
- 标签、状态、提示语必须说明“这个人是谁、为什么在这里、当前需要做什么”，而不是只展示数据库状态。
- 如果文案会让用户疑惑它是来源、状态还是职责，必须重写。
- 文案优化只改展示含义，不借机改业务逻辑、权限逻辑或数据来源。

### 6.2 表格与列表

- 表头名称必须和系统业务名称统一。
- 金额列右对齐，状态列居中，备注类文本左对齐。
- 表格线要完整，底部不能缺横线。
- 不要为了美观造成字段挤压、换行混乱、横向滚动失控。
- 大数据列表必须考虑分页、筛选、搜索、防抖和服务端查询。
- 不要一次性加载成千上万数据到前端。

### 6.3 弹窗与表单

- 弹窗必须有明确标题、关闭按钮、主操作、取消操作。
- 表单必填项要校验并给清晰提示。
- 下拉选项多时不能使用普通长下拉，应使用搜索/弹窗/选择列表。
- 金额和数字输入必须避免无效前导 0，如 `0300`。
- 日期时间必须按北京时间处理。

## 7. 性能规范

系统第一体验目标是轻快。

必须避免：

- 菜单点击后无反馈。
- 页面切换时旧菜单和新菜单同时高亮。
- 大列表一次性全量渲染。
- 大量不必要的接口请求。
- 输入搜索每输入一个字就立即请求接口。
- 无意义的骨架屏堆叠。

推荐做法：

- 点击导航或行进入详情时立即给选中态/加载态。
- 加载可用转圈提示，不使用用户讨厌的复杂骨架屏。
- 搜索使用防抖。
- 列表使用分页。
- 图片和附件延迟加载。
- 大数据查询尽量放到服务端。

## 8. 备份与可回滚规范

以下情况必须先备份：

- 大范围 UI 重构。
- 经营总览、客户管理、工地详情、财务中心等核心页面重构。
- 数据结构调整。
- 权限逻辑调整。
- 财务逻辑调整。
- 导入/导出逻辑调整。
- 用户明确要求“做好备份可以还原”。

备份命名建议：

```text
文件名.backup-before-变更说明-YYYYMMDD.扩展名
```

备份后必须在最终回复中告诉用户备份路径。

## 9. 代码修改规范

- 先阅读现有代码和模式，再动手。
- 改动范围要尽量小。
- 优先复用已有组件、工具函数、样式规范。
- 不做无关重构。
- 不格式化无关文件。
- 不改用户未要求的业务字段和文案。
- 手工编辑文件使用 `apply_patch`。
- 不用临时脚本随意重写源码文件。
- 遇到用户已有改动，不覆盖、不回退，必须顺着现状处理。

## 10. 验证规范

每次改动后按风险选择验证：

- 文案/样式小改：至少运行 `npm run build`。
- 业务逻辑改动：运行构建，并做关键流程自测。
- 接口/数据库改动：检查正常、异常、无权限、重复提交场景。
- 财务/权限改动：必须重点验证边界条件。
- UI 重构：尽量用浏览器查看实际页面。

如果无法运行验证，必须在最终回复中明确说明。

## 11. 上线前检查清单

正式给门店使用前必须确认：

- 线上 HTTPS 域名可用。
- `.env` 生产配置正确且不泄露。
- 数据库有自动备份。
- 附件/图片有备份和访问控制。
- 管理员、门店、员工权限已经测试。
- 客户手机号等敏感信息按权限脱敏。
- 财务收款、退款、冲销流程已用测试数据跑通。
- 导入导出权限受控。
- 出错日志能定位问题但不泄露敏感数据。
- 有回滚方案。
- 有至少一名技术人员或外部顾问能处理线上故障。

## 12. 长期可维护性与模块边界

本系统必须采用渐进式模块化，禁止继续制造难以维护的超大页面和超大接口。

### 12.1 文件体积约束

- 新增页面入口文件应以路由编排、数据装配和模块组合为主，原则上不超过 800 行。
- 新增业务组件原则上不超过 500 行；自定义 Hook、服务和纯计算模块原则上不超过 400 行。
- 超出上述范围时，必须先说明无法继续拆分的原因，不能默认把所有逻辑继续堆进一个文件。
- 已有超过 2000 行的文件不得因为普通功能迭代继续无边界增长；修改其中一个独立 Tab、弹窗或业务流程时，应优先把本次触及的完整模块渐进拆出。
- 不允许为了降低行数进行机械拆文件。拆分后必须形成清楚的业务边界，并减少状态耦合、重复计算或重复 UI。
- 不允许一次性重写客户详情、工地详情、分公司设置、报价详情等核心大页面。必须一次只拆一个可验证模块，并保留可回滚版本。

### 12.2 推荐模块边界

- 多 Tab 详情页：页面入口负责路由、权限和共享数据；每个 Tab 使用独立模块。
- 复杂弹窗：弹窗展示、表单状态和校验逻辑从页面主体拆出，避免在页面文件中继续堆叠。
- 纯计算规则：金额、工期、报价、状态、筛选、导出分组等逻辑放入 `src/lib` 或对应领域模块，不与 JSX 混写。
- 数据访问：接口请求、响应归一化和错误处理优先收敛到已有 API/查询层，不在多个组件中重复拼接。
- 共享 UI：只有确实跨页面复用并且行为一致时才抽公共组件，禁止为了抽象而抽象。
- 组件拆分不得改变字段含义、权限、数据范围、默认值、排序和已有交互。

### 12.3 数据形状与展示规则

- 调用 `.map`、`.filter`、`.find` 等数组方法前，接口响应必须经过 `Array.isArray` 或结构化校验，错误对象不能被当作数组。
- 打印、导出和分享页面必须忠实使用已保存的数据，不得擅自猜测空间、分类、金额、人员、状态等字段；确需推断时必须是明确业务规则并有测试。
- 较长的 `INSERT`、`UPDATE` 语句优先使用命名参数，避免字段和值依赖位置对应；字段增减时必须有结构或回归验证。
- 上传文件依赖业务 ID 时，新增业务数据必须采用“先创建再上传”或可清理的安全草稿方案，不能拿不存在的临时 ID 调正式上传接口。
- 动态上传图、Blob 预览、二维码、打印图和全景图统一走受控原生图片组件；系统静态资源优先使用 Next 图片优化。任何文件读取都必须限制在允许目录并校验登录、权限和公司归属。

## 13. 自动化回归与发布门禁

自动化验证不是可选项。人工浏览用于补充视觉和流程体验，不能替代财务、权限和数据一致性测试。

### 13.1 测试数据隔离

- 自动化测试必须使用临时数据库、数据库副本或会自动回滚的事务。
- 禁止自动化测试直接连接正式数据库或在本地业务数据库 `prisma/dev.db` 中留下测试记录。
- 财务、合同、审批、库存、上传和删除流程的测试必须验证失败场景不会留下部分写入。
- 测试上传文件必须使用独立临时目录，并在测试结束后清理；不得混入正式或本地业务上传目录。
- 不得在测试输出、截图、日志或测试夹具中写入真实手机号、地址、Token、密钥、收款信息和生产数据。

### 13.2 回归覆盖要求

- 每次修复真实缺陷时，应同步增加能够复现该缺陷的回归测试；没有测试框架时，至少提供可重复的只读检查、临时数据库验证或事务回滚验证，并在后续测试阶段补齐。
- 财务写入必须覆盖：字段绑定、金额边界、审批与非审批分支、重复提交、事务回滚和权限拒绝。
- 报价与导出必须覆盖：金额计算、空空间/空分类、排序、优惠税费、打印分组和文件格式。
- 材料与上传必须覆盖：新增后上传、编辑后上传、多图顺序、取消操作、文件不存在、越权访问和构建后新增文件读取。
- 客户与工地流程必须覆盖：无效 ID、错误响应形状、状态推进、模板生成、节点完成和权限数据范围。
- 修复只能在“原问题可复现、修复后测试通过、相邻正常流程未回归”三项都满足后结束。

### 13.3 本地发布门禁

生成发布包前，必须在本地完整通过：

```text
npm run lint
npm run typecheck
npm run check:security
npm run check:database
npm run build
```

- 建立自动化测试命令后，测试命令必须加入上述门禁和 `verify:local`，不得绕过。
- ESLint 必须保持 0 error、0 warning；不能用全局禁用规则掩盖可修复问题。
- 构建成功不代表业务验证完成。核心流程改动还必须完成浏览器回归和用户验收。
- 发布包必须排除 `.env`、数据库、上传文件、备份目录、构建缓存和 macOS 元数据，并生成 SHA-256 校验值。

## 14. 环境隔离与正式发布

- 本地开发、本地测试和正式环境必须明确区分；用户未明确批准发布时，禁止连接或修改正式服务器。
- 本地测试数据和正式业务数据不得相互覆盖。发布代码时禁止上传本地 `.env`、数据库和 `public/uploads`。
- 正式发布前必须备份生产数据库、`.env`、`.env.local`、源码、上传文件和上一版构建，并执行数据库完整性检查。
- 新版本必须先在独立临时目录解压并构建成功，再替换正式环境；禁止把未经服务器构建或验证的代码直接覆盖运行目录。
- 替换前必须记录 PM2 当前状态；替换后必须验证 PM2 `online`、本机端口、HTTPS 公网地址、错误日志和数据库完整性。
- 必须保留上一版构建和明确回滚路径。新版本健康检查失败时先回滚，不在故障中的正式目录里继续试错。
- 正式发布采用一步一确认。对于不熟悉技术的用户，每次只提供当前需要执行的命令，并根据实际输出决定下一步。
- 发布完成后必须记录发布包名称、SHA-256、生产备份路径、旧构建路径和验证结果。

## 15. 对代码助手的特别要求

后续代码助手处理本项目时必须遵守：

- 用户不懂技术，要用清楚、直接、可执行的话解释风险。
- 用户情绪强烈时，先修正问题，不争辩。
- 用户要求“先讨论不开发”时，不改代码。
- 用户要求“开发吧/改吧”时，主动完成实现和验证。
- 涉及数据安全、删除、财务、权限、上线时，必须主动提醒风险。
- 不把“能做”当成“应该做”，要根据装修公司业务场景判断是否合理。
- 每次最终回复简明说明改了什么、文件在哪、验证结果是什么。
